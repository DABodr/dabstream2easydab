This directory can contain local binaries bundled for the application.

The application looks here first for:

- `tools/bin/edi2eti`
- `tools/bin/odr-edi2edi`
- `tools/bin/eti2zmq`

before falling back to the system `PATH`.
